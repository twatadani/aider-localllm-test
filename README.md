# aider-localllm-test
Aiderを介したローカルLLMのテスト

## ディレクトリ構造

各ローカルLLMモデルごとにディレクトリを作成。その内部にaider用の設定ファイルを作成する。

## ローカルLLMに作らせるタスクのリスト

すべてCLI専用、Pythonで作る。

### 初級編

1. 階乗計算  
コマンドライン引数でnを受け取り、nの階乗を計算してターミナルに表示する。  

プロンプト  
「**モデル名**/factorialをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数でnを受け取り、nの階乗を計算してターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはfactorial_main.pyとしてください。メイン実行ファイル以外にモジュールファイルを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示されていない作業は決して行わないでください。」

2. フィボナッチ数列
コマンドライン引数でnを受け取り、フィボナッチ数列(n)を計算してターミナルに表示する。

プロンプト  
「**モデル名**/fibonacciをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数でnを受け取り、フィボナッチ数列のn番目の要素の値を計算してターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはfibonacci_main.pyとしてください。メイン実行ファイル以外にモジュールファイルを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」


3. 文字列逆順
引数の文字列を逆順に表示する

プロンプト  
「**モデル名**/invertをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数で文字列を受け取り、その文字列を逆順にターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはinvert_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

4. GCD計算
コマンドライン引数を2つ受け取り、ユークリッド互除法でそれらの最大公約数を表示する。その際、ユークリッド互除法をCLI上でグラフィカルに表示する。

プロンプト
「**モデル名**/gcdをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数で数値m, nを受け取り、mとnの最大公約数をユークリッド互除法を用いて計算し、ターミナルに出力するPythonスクリプトを作成してください。その際ユークリッド互除法の計算過程もターミナルに視覚的にわかりやすく表示するようにしてください。スクリプトのメイン実行ファイルはgcd_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

5. 引数合計
コマンドライン引数を複数受け取り、それらの合計と平均を表示する。

プロンプト
「**モデル名**/sumaveをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数で複数(1個以上いくつでも)の数値を受け取り、それらの合計と平均値をターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはsumave_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

### 中級編

6. 行/単語カウント
コマンドライン引数でファイルパスを受け取り、ファイルの行数、単語数、文字数を表示する。

プロンプト
「**モデル名**/filecountをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数でファイルのパスを受け取り、そのファイルのサイズ、行数、単語数、文字数をターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはfilecount_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

7. パスワード生成
文字種、長さ指定でセキュアなパスワードの候補を生成

プロンプト
「**モデル名**/makepasswdをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数で文字数nを受け取り、その文字数ぴったりで小文字、大文字、記号、数字をすべて混ぜ込んだセキュアなパスワードをターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはmakepasswd_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

8. CLI電卓
コマンドライン引数を評価する関数電卓

プロンプト
「**モデル名**/calculatorをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数で評価式を受け取り、数式として評価した結果をターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはcalculator_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

9. プログレスバー
n秒カウントダウン中にターミナルにプログレスバーを表示。ETAも表示。

プロンプト
「**モデル名**/progressをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数で秒数nを受け取り、進行度%をプログレスバーとしてターミナルに出力し、1秒ごとに表示を更新するPythonスクリプトを作成してください。なお、プログレスバーの右側に予定残り秒数も表示してください。スクリプトのメイン実行ファイルはprogress_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」


### 上級編

10. ASCIIアート変換
コマンドライン引数の入力テキストをASCIIアートにする

プロンプト
「**モデル名**/asciiartをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数の全てを文字列として受け取り、アスキーアート化してターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはasciiart_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

11. ディレクトリツリー
コマンドライン引数でパスを受け取り、それ以下のディレクトリツリーとそのディスクサイズをターミナル上でグラフィカルに表示する。

プロンプト
「**モデル名**/dtreeをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。ワーキングディレクトリ内にコマンドライン引数をパスとして受け取り、以下のディレクトリ構成とディレクトリのディスクサイズを視覚的にわかりやすくターミナルに出力するPythonスクリプトを作成してください。スクリプトのメイン実行ファイルはdtree_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

12. ソート視覚化
ランダムな数字のリストを生成し、バブルソートする。ステップごとにターミナルに過程を表示する。

プロンプト
「**モデル名**/visuarlsortをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。スクリプトファイルはすべてワーキングディレクトリ内に作成してください。ランダムな整数を20個生成し、昇順にソートするPythonプログラムを作成します。ソートアルゴリズムはコマンドラインオプションで-bが与えられたときはバブルソート、-qが与えられた時はクイックソート、-mが与えられた時はマージソートを行ってください。ソートの1ステップごとに現在のリストの状態を視覚的に分かりやすくターミナルに表示してください。スクリプトのメイン実行ファイルはvsort_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」


13. 五目並べ
ユーザーと対話的に五目並べを行う。

プロンプト
「**モデル名**/gomokuをワーキングディレクトリとします。ワーキングディレクトリが存在しなければ作成してください。スクリプトファイルはすべてワーキングディレクトリ内に作成してください。ターミナル上でユーザーと対話的に五目並べゲームを行うPythonプログラムを作成してください。ユーザーが先手か後手かを起動時にユーザーに尋ねるようにしてください。スクリプトのメイン実行ファイルはgomoku_main.pyとしてください。メイン実行ファイル以外にモジュールファイルやサブディレクトリを作成しても構いません。ワーキングディレクトリより上位のディレクトリには決して変更を加えないでください。処理についてのコメントはプログラミング初心者が読んでもわかるように親切に書いてください。docstringも丁寧に書いてください。コメントやdocstring、ターミナル出力の自然言語部分は日本語で書いてください。README.mdに記載されたタスクごと、エンジンになるAIモデルごとに段階的にプロジェクトを作成しているため、たとえコンテキストに含まれていても今回のプロンプトで指示された以外の作業は決して行わないでください。」

